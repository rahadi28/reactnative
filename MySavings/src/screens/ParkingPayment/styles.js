import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonScan: {
    width: 200,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  cameraView: {
    height: 600,
    width: '100%',
  },
});
